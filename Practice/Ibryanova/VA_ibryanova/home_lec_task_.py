#Есть список списков (матрица). Каждый внутренний список –
# это строка матрицы. Необходимо реализовать функцию,
# которая удаляет столбец, который содержит заданную цифру.


def find(lst, x):
    lst_inx = [1]
    return lst_inx

def delete(lst, lst_to_remove):
    print(lst)
    print(lst_to_remove)


lst = [
    [1, 2, 1],
    [1, 1, 1],
    [1, 1, 1],
]

res = find(lst, 2)
delete(lst, res)
